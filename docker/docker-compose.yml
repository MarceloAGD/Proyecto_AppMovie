version: '3.7'

services:
  app:
    container_name: nestjs-docker
    build:
      context: ..
      dockerfile: docker/Dockerfile

    ports:
      - 4000:4000

    volumes:
      - ../:/usr/src/app

